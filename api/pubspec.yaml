name: reachtrail_api
description: ReachTrail backend API for auth and external provider proxying.
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  crypto: ^3.0.6
  http: ^1.6.0
  jose: ^0.3.5+2
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
