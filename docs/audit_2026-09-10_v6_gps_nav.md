# 版 6 実機確認（Pixel_8 エミュレーター、2026-09-10）

擬似位置: 東京駅（`adb emu geo fix 139.7671 35.6812`）。

| # | 確認 | 結果 |
|---|------|------|
| 1 | 基準地点タブ「現在地を使う」→ 権限ダイアログはボタン押下時のみ → 許可で地図が東京駅へ移動 | OK |
| 2 | 権限拒否 → 「位置情報の利用が許可されていません…」バナー | OK |
| 3 | 登録タブ、基準地点なし → 起点「現在地」が自動選択、検索可能、結果タグ「現在地から N m」 | OK（初回ビルドで検索ボタンが無効のままになる不具合を修正済み） |
| 4 | 「地図アプリで開く」→ Google マップが店名・座標付きで起動 | OK |
| 5 | 基準地点なしで「この候補で記録」→ 案内 SnackBar、シートは開かない | OK |
| 6 | 右上の ? → 使い方ガイド 8 セクション | OK |

備考: Play 開発者サービスの「Location Accuracy」を断ると fused 取得が失敗するため、Android では LocationManager へのフォールバックを追加した。
